/// NotificationService is a stub until Firebase is configured.
/// Run `flutterfire configure` then uncomment the real implementation.
class NotificationService {
  static Future<void> initialize() async {
    // No-op until Firebase is configured
  }

  static Future<String?> getToken() async => null;
}
